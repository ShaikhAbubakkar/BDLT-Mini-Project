PORT=5000
NODE_ENV=development
IPFS_HOST=localhost
IPFS_PORT=5001
IPFS_PROTOCOL=http

# Note: Must run 'ipfs daemon' before starting backend
# Install IPFS: https://docs.ipfs.tech/install/command-line/
